import styled from 'styled-components/native'

type ButtonIconVariantProps = 'success' | 'danger'

export const Container = styled.TouchableOpacity`
  width: 56px;
  height: 56px;
  justify-content: center;
  align-items: center;
  margin-left: 12px;
`
